To run the project tests, use the code snippet below.

```bash
yarn test
```

To see the coverage report, use the flag --coverage.

```bash
yarn test --coverage
```

In addition to being shown in the terminal, a folder will be created at the root of the project.
